extensionId: templateExample # This needs to be unique
buildType: docker
language: mypython
creationType: template
info:
  provider: IBM
  version: 0.0.1

# Information to display in the UI 
userInterface:
    label: myPython hello world template
    description: HelloWorld microservice written in python

# Specify location of template code, either gitURL or location relative
# to this .yaml file
template:
  giturl:
  localpath: .

# Additional details about the application
application:
  port: 5000  # The port the application runs on
